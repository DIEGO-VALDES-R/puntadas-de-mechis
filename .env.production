# Variables de entorno para el despliegue en Vercel (Producción)

# Base de Datos (PostgreSQL)
# Asegúrate de configurar tu base de datos en Vercel o un proveedor externo.
# La URL de conexión debe ser la que te proporciona el proveedor.
DATABASE_URL="postgresql://user:password@host:port/database"

# Claves de Autenticación (JWT)
# Genera una clave secreta fuerte para la firma de tokens.
JWT_SECRET="tu_clave_secreta_jwt_aqui"

# Configuración de S3 (AWS)
# Necesario para la subida de imágenes.
AWS_ACCESS_KEY_ID="tu_aws_access_key_id"
AWS_SECRET_ACCESS_KEY="tu_aws_secret_access_key"
AWS_REGION="tu_aws_region"
AWS_BUCKET_NAME="tu_aws_bucket_name"

# Configuración de OpenAI (Opcional, si se usa)
OPENAI_API_KEY="tu_openai_api_key"

# URL de la API (Se configura automáticamente en Vercel, pero se deja como referencia)
# VITE_API_URL="https://[nombre-proyecto].vercel.app/api"
